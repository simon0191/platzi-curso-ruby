# Utilizando lo que aprendiste sobre el código ASCII, completa el método `encode` para que modifique
# el texto que recibe como parametro y haga que cada uno de sus caracteres se transforme en el
# siguiente caracter.

def encode(text)
  #TODO: Implementa este metodo
end

if encode("abcd$@") == "bcde%A"
  puts "~~(0__0)~~ Good job!"
else
  raise "INCORRECT"
end

